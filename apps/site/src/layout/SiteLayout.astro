---
import BaseLayout from "@/layout/BaseLayout.astro";
import Navbar from "@/components/navbar/index.astro";
import Footer from "@/components/common/footer.astro";
interface Props {
  class?: string;
}
let { class: classes }: Props = Astro.props;
---

<BaseLayout
  theme="dark"
  class="font-sans isolate bg-black min-h-screen h-full min-h-screen flex flex-col items-center"
>
  <div
    class="w-full grid place-items-center fixed px-5 max-w-screen-xl isolate z-1"
    transition:persist
  >
    <Navbar />
  </div>
  <main
    class=`isolate w-full grid place-items-center px-5 max-w-screen-xl ${classes} `
    transition:animate="slide"
  >
    <slot />
  </main>
  <Footer class="max-w-screen-xl" />
</BaseLayout>
